import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    display: 'flex',
  },
  title: {
    fontSize: 24,
    marginBottom: 10,
    marginTop: 10,
  },
  section: {
    paddingTop: 20,
    paddingBottom: 20,
  },
});
